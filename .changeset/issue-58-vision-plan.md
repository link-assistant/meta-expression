---
'my-package': patch
---

Sync the documentation with the actual state of the code and plan the full
vision as GitHub issues (issue #58). Refresh `docs/ROADMAP.md` with per-phase
status markers and known blockers, extend `docs/REQUIREMENTS.md` with the
formal-ai era requirements (R50–R57), add a single-page `docs/IMPLEMENTATION-STATUS.md`
vision tracker, and add the `docs/case-studies/issue-58/` analysis (audit,
requirements, online research, solution plan, issue plan). No runtime, API, or
behaviour changes.
